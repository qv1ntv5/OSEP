var shell = new ActiveXObject("WScript.Shell");
var res = shell.Run("<PATH>");

// IN SUMMARY; this script uses the ActiveX tech to that creates a WScript.Shell object constructor which is used to run an application with .run method.